
busttime = []
     
print("Enter the number of process: ")
 nnumber = int(input())

processes = []
for index in range(0,number):
    processes.insert(index,index+1)

print("Enter the burst time of the processes: \n")
  busttime = list(map(int, raw_input().split()))

  for index in range(0, len(busttime)-1):      
   for j in range(0, len(busttime)-index-1):
    if (busttime[j] > busttime[j+1]):
     temp = busttime[j]
     busttime[j] = busttime[j+1]
     busttime[j+1] = temp
     temp = processes[j]
     processes[j] = processes[j+1]
     processes[j+1] = temp

waittime = []   
avgwt = 0  
turnaroundtime = []   
avgtat = 0 
  
waittime.insert(0, 0)
turnaroundtime.insert(0, busttime[0])

for index in range(1, len(busttime)):   
  waittime.insert(index, waittime[index-1] + busttime[index-1])
  turnaroundtime.insert(index, waittime[index] + busttime[index])

 avgwt += waittime[index]
 avgtat += turnaroundtime[index]
 avgwt = float(avgwt)/number
 avgtat = float(avgtat)/number

print("\n")

print("Process\t  Burst Time\t  Waiting Time\t  Turn Around Time")

for index in range(0, number):
print (str (processes[index]) + "\t\t" + str(busttime[index]) + "\t\t" + str(waittime[index]) + "\t\t" + str(turnaroundtime[index]))
print("\n")

print("Average Waiting time is: "+str(avgwt))
print("Average Turn Arount Time is: "+str(avgtat))